---
name: 点点demo
status: draft
last_updated: 2026-06-25
used_by: [docs-pages]
---

# 点点demo

## 需求背景

点点demo 是外部业务方提供的独立页面，用来验证「Figma 规范 → 本仓库页面 demo → 本地/线上预览」这条流程能否跑通。它不属于 MindBack App 的内部业务页面，当前只借用文档站的页面承载和预览机制。

视觉以 Figma `Dots 规范` 为准：默认历史消息态参考节点 `1421:1089`，点击键盘后的输入态参考节点 `1419:970`，选中 Skill 后的输入态参考节点 `1422:2458`。代码不得按 MindBack 业务逻辑或 MindBack 消息组件自行改版。

## 页面结构

1. 状态栏
2. 顶部导航：左侧返回，居中标题「点点」，右侧更多
3. 空白对话区
4. 底部横向快捷提示：穿搭指南、好物买手、旅行攻略、上帝摄像机
5. 底部输入栏：添加、按住说话、键盘入口、相机入口
6. AI 生成标注
7. 输入态：Dots 历史消息流、输入占位、工具按钮、系统键盘
8. 选中 Skill 输入态：Skill 名称、关闭入口、输入框、工具按钮、系统键盘

## 视觉规格

- 画板宽度 393px，高度 852px。
- 页面背景为浅灰底，顶部和内容区不新增卡片或分割线。
- 中部保持大面积空白，不新增内容卡片。
- 底部输入状态整体高度 144px。
- 快捷提示行高度 40px，左右边距 20px，item 间距 8px。
- 快捷提示为白色 pill，高 40px，左图标 20px，文字 14px Regular，字距 0.56px。
- 输入区高度 96px，背景为浅灰，顶部圆角 28px，顶部内边距 10px。
- 语音输入行高度 48px：左侧添加区域 54px，中间语音按钮自适应，右侧键盘/相机区域 76px。
- 语音按钮为白色圆角矩形，圆角 16px，文字「按住说话」居中。
- 按住语音按钮后进入语音态：隐藏快捷提示、添加、键盘、相机和 AI 生成标注；语音按钮宽度变为 285px，内部展示动态波形。
- 语音态顶部提示默认「松手发送，上移取消」；手指上移超过 48px 后提示切换为「松手取消」，波形降弱。
- 语音识别期间，在消息区底部追加右侧用户识别气泡；未识别出文字前展示三点 loading，识别后逐段替换成文字。
- 松手发送后，识别文字作为用户消息进入历史消息流；上滑取消或 pointer cancel 时移除识别气泡，不发送消息。
- 底部水印高度 38px，显示「内容由 AI 生成」和 iOS home indicator。
- 点击键盘入口后进入输入态：输入面板从 y=393.274px 开始，高 458.726px。
- 历史消息流按 Figma `1421:1089` 展示 Dots 消息体：User 右侧灰底气泡、Dots 左侧白底气泡、AI 生成卡片。
- 历史对话文案围绕 AI 生成卡片内容模拟真实护肤咨询：用户询问黑绷带面霜用法、厚涂和频率，Dots 先给简短结论，再展示生成卡片。
- 消息内容区为独立纵向滚动容器：默认态高度 575px，输入态高度 264px，选中 Skill 输入态高度 240px；隐藏滚动条，保留触控惯性滚动。
- 键盘拉起、选中 Skill、发送新消息后，消息内容区自动滚动到底部，保证最新消息露出在输入面板上方。
- 输入态「给点点发消息」区域为真实输入框，可输入文本。
- 输入态底部键盘使用 Figma 导出的本地键盘整图。
- 输入态点击上方空白对话区关闭键盘，回到默认底部栏，保留已输入内容。
- 输入态有文本内容时，右侧语音菜单和相机入口替换为发送按钮。
- 点击底部快捷提示胶囊后自动拉起输入态，并在输入框上方展示已选 Skill。
- 已选 Skill 行：左侧 20x20 图标 + Skill 名称，右侧 24x24 关闭入口；点击关闭入口只移除已选 Skill，不关闭输入态。
- 点击发送后追加到历史消息流并自动收起键盘：用户消息在右侧，Dots 回复在左侧，样式参考 Figma `1421:1089`；若本次发送带已选 Skill，发送完成后清空已选 Skill。
- 按住说话、键盘输入、快捷提示选择、发送、关闭 Skill 都必须保留；新增业务能力只能叠加在点点对话页基座上，不能替换基座交互。
- 用户消息气泡：右对齐，最大宽 293px，背景 `#E6E6E6`，0.5px 白色 75% 边框，圆角 22px，内边距 12px 16px。
- Dots 回复气泡：左对齐，最大宽 293px，背景白色，0.5px 白色 20% 边框，圆角 22px，内边距 12px 16px。
- 消息文字：PingFang SC Regular，16px，行高 26px，字距 2%。
- AI 生成卡片参考 Figma `1421:1105`：宽 353px，白色 80% 背景，圆角 36px，内边距 26px 22px 32px，内容间距 26px；顶部三张 105px 圆角图片，下方为「正确使用手法」和「注意事项」内容块。

## 切图清单

当前代码已接入 `src/assets/dotted/` 下的切图资源：

| 资源名 | 位置 | 尺寸 |
|--------|------|------|
| `dots-chip-outfit.svg` | 快捷提示「穿搭指南」左侧 | 20x20 |
| `dots-chip-goods.svg` | 快捷提示「好物买手」左侧 | 20x20 |
| `dots-chip-travel.svg` | 快捷提示「旅行攻略」左侧 | 20x20 |
| `dots-chip-camera-guide.svg` | 快捷提示「上帝摄像机」左侧 | 20x20 |
| `dots-chip-*-solid.svg` | 选中 Skill 行图标 mask，颜色继承文字 | 20x20 |
| `dots-action-add-circle.svg` | 输入栏左侧添加入口 | 24x24 |
| `dots-action-keyboard.svg` | 输入栏右侧键盘入口 | 24x24 |
| `dots-action-camera.svg` | 输入栏右侧相机入口 | 24x24 |
| `dots-action-message-menu.svg` | 输入态右侧语音菜单 | 24x24 |
| `dots-action-send.svg` | 输入态有文本时右侧发送入口 | 28x28 |
| `dots-message-tail.svg` | 输入态历史气泡尾巴 | 24x24 |
| `dots-keyboard-ios-latest.png` | 输入态系统键盘，来自 Figma `1429:2088` | 402x322 |
| `dots-ai-card-1.png` | AI 生成卡片左侧图片 | 105x105 展示 |
| `dots-ai-card-2.png` | AI 生成卡片中间图片 | 105x105 展示 |
| `dots-ai-card-3.png` | AI 生成卡片右侧图片 | 105x105 展示 |

## 触觉反馈

| 交互 | 意图 |
|------|------|
| 点击快捷提示 | tick |
| 点击已选 Skill 关闭入口 | light |
| 点击添加/键盘/相机 | light |
| 按住说话 | medium |
| 语音松手发送 | success |
| 语音上滑取消 | light |
| 输入态点击语音菜单 | 返回无焦点态 |
| 输入态点击上方空白区 | 关闭键盘 |
| 输入态点击发送 | success |

## 产品方案演示扩展：任务创建

`dotted-task-demo` 是基于点点对话页基座的独立全屏 H5 demo，用来汇报「对话中创建持续任务」的产品方案。它只新增任务相关内容，不改变点点原有输入、快捷提示、键盘、语音、发送和消息滚动逻辑。

### 页面容器

- 演示页最外层使用独立背景，必须与手机页面背景拉开差距。
- 手机页面固定 393px × 852px，不随桌面窗口拉伸。
- 手机页面容器圆角为 40px；这是外部点点演示页的局部规格，不写入 MindBack 全局 radius token。
- 所有汇报提示、重置、重新演示、模拟到价等控制都放在手机页面外部，不进入点点页面。
- 外部控制按钮只作为演示推进器，使用 Button `variant=neutral` + `size=medium`；页面内确认创建按钮才是业务主操作。

### 任务卡片

- 任务卡片出现在 Dots 回复之后，仍在对话消息流内。
- 卡片标题为任务对象，例如「北京 → 东京」。
- 卡片内容至少包含出发日期、价格阈值、偏好和通知方式。
- 卡片不显示「等待确认」状态 pill，避免和确认按钮形成重复信息。
- 「确认创建」按钮必须放在任务卡片内部右下角，使用 Button `variant=filled` + `size=medium`。
- 点击确认创建后，卡片进入已创建状态，并由 Dots 追加简短确认回复。

### 通知模拟

- 到价触发由页面外部控制区模拟，不能在手机页面内新增调试按钮。
- 模拟通知出现在点点页面内，位置靠近顶部，使用点点消息/浮层视觉，不使用系统设置页或 MindBack 内部卡片样式。

## 依赖组件

| 组件 | 状态 | 用途 |
|------|------|------|
| 页面容器 | 已实现 | 借用文档站 PagePage 预览机制 |
| 状态栏 | 已实现 | 借用 MobileShell 状态栏 |

## 多宽度适配

| 宽度 | 规则 |
|------|------|
| Mobile 393px | 当前预览规格 |
| Tablet | 保持手机画板预览，不拉伸页面 |
| Desktop | 保持手机画板预览，不拉伸页面 |
